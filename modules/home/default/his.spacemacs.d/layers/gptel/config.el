;; (defvar gptel-backend "ChatGPT"
;;   "LLM Backend.")

;; (defvar gptel-model "gpt-3.5-turbo-1106" "LLM Model.")

;; (defvar gptel-max-tokens 16 "Maximum number of tokens to (input + output).")

;; (defvar gptel-temperature 0 "Model temperature (randomness).")

;; (defvar gptel-api-key nil "API key for backend API.")
